<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Antiquités Marketplace</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Georgia:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <nav>
            <a href="/" class="logo">
                <i class="fas fa-gem"></i> Antiquités
            </a>
            <ul class="nav-links">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.isAdmin) { %>
                        <li><a href="/admin"><i class="fas fa-cog"></i> Administration</a></li>
                    <% } else { %>
                        <li><a href="/dashboard"><i class="fas fa-tachometer-alt"></i> Mon espace</a></li>
                        <li><a href="/create-announcement"><i class="fas fa-plus"></i> Créer une annonce</a></li>
                    <% } %>
                    <li><a href="/logout"><i class="fas fa-sign-out-alt"></i> Déconnexion</a></li>
                <% } else { %>
                    <li><a href="/login"><i class="fas fa-sign-in-alt"></i> Connexion</a></li>
                    <li><a href="/register"><i class="fas fa-user-plus"></i> Inscription</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main class="container fade-in">
        <% if (typeof message !== 'undefined' && message) { %>
            <div class="alert alert-<%= typeof messageType !== 'undefined' ? messageType : 'info' %>">
                <i class="fas fa-info-circle"></i> <%= message %>
            </div>
        <% } %>
        <%- body %>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Antiquités Marketplace. Découvrez l'histoire à travers nos trésors.</p>
    </footer>

    <script>
        // Animation d'apparition pour les éléments
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.announcement-card, .form-container, .admin-dashboard');
            cards.forEach((card, index) => {
                setTimeout(() => {
                    card.classList.add('fade-in');
                }, index * 100);
            });
        });
    </script>
    <script src="/js/app.js"></script>
</body>
</html>
